# Use your local Postgres role for Unix-socket auth. In most local setups this is
# the same value as `whoami`.
# Example: DATABASE_URL="postgresql://naveenprasath-p@localhost/dotly_one?host=/var/run/postgresql"
DATABASE_URL="postgresql://YOUR_LOCAL_POSTGRES_ROLE@localhost/dotly_one?host=/var/run/postgresql"
